## Goal
- Repair Character Inspector damage and ensure all inspectors meet per-resource save-as menu requirements with proper CLIM gadgets and layout per specification.

## Constraints & Preferences
- Must not hallucinate data structures; use actual game-resource-character fields and structure
- CLIM gadgets must be used for in-window controls (radio-box-pane, check-box-pane, slider-pane)
- Unicode characters ☑ ◯ ⬤ required for menu items
- Each inspector must have per-resource save-as menu (not inherit generic `inspector-save-as-menu`)
- Tabbed interface required: Basic Data, Stats, Appearance, Inventory, Keys, Flags
- Read-only view uses CLIM formatting-table; editable view uses interactive gadgets
- Error signaling uses proper CLIM `error` conditions, NOT stream output

## Progress
### Done
- Restored gui-blob.lisp to working state with blob-save-as-menu (Text/JSON/PDF/POV-Ray/WebM)
- Restored gui-boat.lisp to working state with boat-save-as-menu (Text/JSON/PDF/PNG/WebM)
- Restored sprite-sheet-inspector.lisp with sprite-sheet-save-as-menu (Text/JSON/PDF/PNG)
- Fixed Character Inspector (`gui-character.lisp`) completely:
  - ✔ Removed phantom `character-data` struct
  - ✔ Uses actual `game-resource-character` fields exclusively
  - ✔ Proper CLIM tabbed interface with 6 tabs (Basic Data, Stats, Appearance, Inventory, Keys, Flags)
  - ✔ Read-only view uses CLIM formatting-table for all fields
  - ✔ Editable view uses interactive CLIM gadgets (text-input-pane, slider-pane, radio-box-pane, check-box-pane)
  - ✔ Tab switching via View menu submenu with proper command tables
  - ✔ Per-resource `character-save-as-menu` (Text/JSON/PDF)
  - ✔ `write-character-atomically` using temp file + rename pattern
  - ✔ AtariVox speech export with serial port discovery and phoneme encoding
  - ✔ Minifont name validation (12-byte limit) using `error` conditions
  - ✔ All error signaling uses proper CLIM `error` conditions, not stream output
  - ✔ `com-character-save`, `com-character-save-text`, `com-character-save-json`, `com-character-save-pdf` all use `clim-simple-echo`

### In Progress
- Script Inspector PDF export fix to follow Hollywood screenplay format
- Verify all inspectors have proper per-resource save-as menus

### Blocked
- None

## Key Decisions
- Each resource inspector requires its own dedicated save-as menu command table
- CLIM formatting-table must be used for tabular presentation, not format() output
- Tabbed interface uses `frame-current-tab` slot + `display-current-tab` dispatch function
- Error signaling uses `error` conditions, never stream output
- All editing uses proper CLIM gadgets (text-input-pane, slider-pane, radio-box-pane)

## Critical Context
- Character Inspector tabbed interface with:
  1. Basic Data tab: Name, ID, decal, gender, memo
  2. Stats tab: HP, max HP, AC, crowns, arrows, potions, chalice
  3. Appearance tab: Hair color, skin color, clothes color, head, body, speech pitch/speed/bend/color, nicks
  4. Inventory tab: Equipment, shield
  5. Keys tab: Nicks field (from game-resource-character)
  6. Flags tab: OOP-managed flags (future enhancement, references eightbol/oop-character.lisp)
- Each tab has both read-only (viewing) and editable modes
- Editable mode uses CLIM gadgets with `:activation-callback` or `:callback` for real-time updates
- All save/export operations use `clim-simple-echo` for status feedback
- Error conditions use `error` with proper condition types (`minifont-name-too-long`)

## Relevant Files
- /home/brpocock/Projects/Phantasia/SkylineTool/src/gui/gui-character.lisp: Fully rebuilt with tabbed interface
- /home/brpocock/Projects/Phantasia/SkylineTool/src/gui/gui-blob.lisp: Fixed with per-resource save-as menu
- /home/brpocock/Projects/Phantasia/SkylineTool/src/gui/gui-boat.lisp: Fixed with per-resource save-as menu
- /home/brpocock/Projects/Phantasia/SkylineTool/src/gui/sprite-sheet-inspector.lisp: Fixed with per-resource save-as menu
- /home/brpocock/Projects/Phantasia/SkylineTool/src/game-resource.lisp: Defines actual game-resource-character structure
- /home/brpocock/Projects/Phantasia/SkylineTool/.ANCHORED_SUMMARY.md: Updated